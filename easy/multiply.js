/**
 *
 * Create a function that takes two items as arguments and multiply those numbers.
 * The inputs will always be numbers, and you will not need to convert any strings
 * to numbers. You will always be given two arguments, and neither will ever be null.
 *
 * Return the product of the two numbers.
 */

function multiplyTwoNumbers(number1, number2) {
    // solution here

    // write the value of the product after return
    return null;
}

module.exports = multiplyTwoNumbers;
